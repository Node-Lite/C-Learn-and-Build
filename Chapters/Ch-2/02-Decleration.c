#include <stdio.h>

enum Random {Max=100, Min , Mas};
int main(){
    int low, high;
    int a;
    int b;
    char arr[1000];
    int c = Min + 3;
    const int m = 12;
    //m=13;not allowed

}

/*Notes > Internals/Local var have garbage values when undefined
        > Externals/global when undefined has 0
        > const does not allow values to change could be used for var as well as array
        > const could also be passed as argument for arrays so that it values does not changes.*/
